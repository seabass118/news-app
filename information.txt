NEWS APP PLAN

// Development

- Client Side or Server Side ? Client 
- Framework or no framework ? YE
- Auth or No Auth ? NO
- Local Database or Database platform ? NO

// Deployment and testing

- Build for scalabilty ? NO
- Utilise testing ? YES
- Deploy in live enviroment ? NO
- Serverless or VPS ?

// Api

- News service API ? newsapi.org
- Cron jobs ? NO
- Request structure ?
- Call from client or server ? Client
- Cost structure ? (likely not needed)

// Version Control

- Use git ? (yes)
- What control pattern should we use ? (Commit, Request, Merge)
- What branch patterns should we use ? (Dev + Release only)